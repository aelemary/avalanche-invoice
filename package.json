{
  "name": "avalanche-invoice",
  "private": true,
  "scripts": {
    "start": "node --env-file-if-exists=.env server/index.mjs",
    "test": "node --test server/*.test.mjs",
    "install:browser": "playwright install chromium"
  },
  "engines": {
    "node": ">=22"
  },
  "dependencies": {
    "@fontsource-variable/inter": "^5.3.0",
    "playwright": "^1.63.0"
  }
}
